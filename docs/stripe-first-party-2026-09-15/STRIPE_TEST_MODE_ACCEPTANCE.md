# Acceptance evidence

## Executed local qualification

- `npm run test:stripe-first-party`: money, adapter, provisioning replay, prospective/historical legal integrity, proxy, Stripe boundaries, Worker isolation, operator, Job Post, services/sponsorship/assistance, lifecycle, observability, frontend and navigation contracts.
- `npm run typecheck` and `npm run typecheck:functions`.
- Disposable Supabase replay of all 74 migrations with historical behavior fixtures and plpgsql_check of current governed functions.
- Separate clean replay of all 74 migrations plus first-party qualification/environment/deduplication/late-settlement/immutable-history/fee-reduction/Support-to-compute fixtures.
- Production build and focused disabled-payment/Creator Studio browser checks are recorded with deployment evidence.

## Actual Stripe sandbox — NOT RUN / BLOCKED

No secure test credentials, registered endpoint signing secret or isolated economic test runtime were available. Synthetic provider responses and offline database fixtures are not provider acceptance.

Run every master-plan case after configuration: preset/custom Support success; min/max/tampering rejection; checkout cancel/decline; unpaid/pending then delayed success/failure; session and webhook replay; invalid signature; receipt and acknowledgment; zero authority/compute effects; recurring creation/customer/invoice/failed invoice/Portal/period-end cancellation; subscription replay; ordinary membership preserved; Job Post free/exempt/waived/reduced/$10/rejected/failed/refund paths with separate publication review; authorized service/agreement-only payments and refunds; sponsorship only if a real governed lane exists; cross-account/capability denial; mode/key/object mismatch and hard-off third-party endpoints.

The offered payment method is card only. Async events are handled defensively; enabling additional payment methods needs acceptance for those methods. For delayed behavior, use documented Stripe sandbox scenarios where available and signed provider test events, keeping synthetic state-machine tests separately labeled.

Use Stripe test cards in hosted Checkout, never raw PAN in application logs or fixtures. Record opaque test IDs, mode, expected/actual status, event delivery/replay outcome, database ledger assertions and artifact hashes. Never record full webhook bodies, receipt URLs, customer PII or credentials. Do not populate qualification timestamps until these tests actually pass.
