# Local Install Authority

The website may prepare catalog records and install invitations. It cannot install locally.

Local Elysia validates manifests, package hashes, permissions, revocation status, and user consent before local installation or enablement. Local install history does not sync back unless the user explicitly opts in.
